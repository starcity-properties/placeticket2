# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'Placeticket' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Firebase'

  # Pods for Placeticket

  target 'PlaceticketTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PlaceticketUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
